#include "main.h"
#include <stdio.h>

/**
 * main - function that takes a pointer to an int as parameter
 * updates the value it points to to 98
 *
 * Return: Always 0
 */
int main(void)
{
	int n;
	int *p = &n

	*p = 98
	
	printf("%p\n");

	return (0);
}
